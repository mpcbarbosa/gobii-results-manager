# Database
# For Docker (recommended for development):
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/gobii?schema=public"

# For local PostgreSQL installation:
# DATABASE_URL="postgresql://user:password@localhost:5432/gobii_results_manager?schema=public"

# Next.js
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Node Environment
NODE_ENV="development"

# API Authentication
# Generate secure tokens for different API access levels
# Example: openssl rand -hex 32

# Ingestion API (write access for Gobii agents)
APP_INGEST_TOKEN="your-secure-ingest-token-here-change-in-production"

# Read API (read access for querying leads)
APP_READ_TOKEN="your-secure-read-token-here-change-in-production"
